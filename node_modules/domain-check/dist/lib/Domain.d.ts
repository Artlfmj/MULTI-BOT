export declare class Domain {
    private domainUrl;
    private whoisSearch;
    constructor(domain: string);
    static isFree(domain: string): Promise<boolean>;
    private getWhois;
    isFree(): Promise<boolean>;
    private sleep;
}
